=== Getting the Files

Use Git to clone the {booksquisher-templates} to your computer. Your local machine will need to have {git-scm} installed to do this:

```console
$ git clone --depth 1 https://github.com/mmarmor/booksquisher-example-book-templates.git
```

Get the {booksquisher-docker-hub-image} with `docker pull`:

```console
$ docker pull marmor/booksquisher
```
